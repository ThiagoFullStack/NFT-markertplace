import '../ui/StyledButton.module.css';
import styled from 'styled-components';

const StyledButton = styled.button`
  
  &:hover {
    background-color: var(--background-orange-hover); 
  }
`;

export default StyledButton;
